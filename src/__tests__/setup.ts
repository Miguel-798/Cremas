/**
 * Setup file for frontend tests
 *
 * This file runs in Node environment.
 * MSW server setup is handled by node.setup.ts.
 */
// Note: Browser mocks (localStorage, matchMedia) removed
// as we're running in Node environment for API testing.
